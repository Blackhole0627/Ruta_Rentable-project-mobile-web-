# Stage 2 — Supabase
# Leave the placeholders to run fully offline against the built-in mock backend.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Comma-separated emails granted the admin role (admin web panel access).
VITE_ADMIN_EMAILS=admin@rutarentable.com
