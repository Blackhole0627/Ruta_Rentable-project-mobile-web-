node_modules/
dist/
.env
.env.local
.env.*.local
*.log
.DS_Store
.idea/
.vscode/
android/
ios/
.capacitor/

# Temp / cache / build metadata — local only, never deploy
.tmp/
.cache/
.turbo/
coverage/
*.tsbuildinfo

# Vercel CLI local files (project link, pulled env) — keep out of the repo
.vercel

# Android signing secrets (in case any live outside the ignored android/ dir)
*.keystore
*.jks
local.properties

# Unrelated third-party tooling
Skills/

# Build artifacts / large binaries — not needed in repo
RutaRentable.apk
*.apk

# Internal docs — not for the public repo
RutaRentable_Master_Prompt.md
requirement(yamamoto) (1).docx
*.docx

# Database migrations — applied directly on Supabase, kept out of the repo.
# NOTE: supabase/seed/ stays tracked — the web app imports those JSON files
# at build time (apps/web/src/core/constants/catalog.ts), so removing them
# breaks the Vercel build. Edge Functions under supabase/functions also stay.

# LAFISE API docs — contains real credentials, keep local only
docs/lafise docs.txt
